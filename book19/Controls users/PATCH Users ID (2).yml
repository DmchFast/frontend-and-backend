info:
  name: PATCH Users ID (2)
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/users/2
  body:
    type: json
    data: |-
      {
        "age": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
