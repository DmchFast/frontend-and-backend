info:
  name: POST Sign in base user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/users
  body:
    type: json
    data: |-
      {
        "first_name": "Fast",
        "last_name": "Tra1n",
        "age": 19
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
